import { id } from "date-fns/locale"
import { useMoralis, useMoralisQuery } from "react-moralis"

export default function handler(req, res){
    /*const { data, error, isLoading } = useMoralisQuery("Users");

    if (error) {
      return <span>🤯</span>;
    }
    
    if (isLoading) {
      return <span>🙄</span>;
    }
    
    return <pre>{JSON.stringify(data, null, 2)}</pre>;

    res.status(200).json({ text: test('123asd') })
    res.status(200).json({text: allUsers()})*/

    return 'test'
}